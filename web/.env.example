# Anthropic API key (required for evaluation + PDF generation)
ANTHROPIC_API_KEY=your-api-key-here

# Optional: override model
ANTHROPIC_MODEL=claude-sonnet-4-6

# Server bind (localhost by default)
HOST=127.0.0.1
PORT=8787

